# شروع پروژه با MASEF 0.3.1

## ۱. پروژه را بسازید

برای افزونه WordPress:

```bash
python scripts/bootstrap_project.py \
  --name "نام افزونه" \
  --slug "plugin-slug" \
  --profile wordpress-plugin \
  --risk standard \
  --output ../plugin-slug
```

سپس:

```bash
cd ../plugin-slug
python scripts/validate_framework.py
```

## ۲. ایده را به خلاصه محصول تبدیل کنید

ChatGPT باید از ایده کوتاه مالک محصول این موارد را استخراج و تکمیل کند:

- مسئله و هدف واقعی
- مخاطب و سناریوی استفاده
- قابلیت‌های `essential`، `important` و `future`
- رفتارهای قابل تنظیم از پنل
- محدودیت‌های کسب‌وکار
- حالت اجرای `light`، `standard` یا `critical`
- معیارهای پذیرش اصلی
- بسته تحویل مورد انتظار

سؤال فنی که با کد، مستندات، پژوهش یا آزمون قابل پاسخ است نباید از مالک محصول پرسیده شود.

## ۳. خط مبنای WordPress را راستی‌آزمایی کنید

برای پروفایل `wordpress-plugin`، پیش از تأیید خلاصه محصول:

1. WordPress.org Release Archive را بررسی کنید؛
2. خبر رسمی انتشار پایدار را بررسی کنید؛
3. نسخه پایدار را از Alpha، Beta و RC جدا کنید؛
4. مقدار دقیق نسخه پایدار را در منشور پروژه ثبت کنید؛
5. همان مقدار را در هدر، `readme.txt`، CI، آزمون، Issue و PR استفاده کنید.

مقدار راستی‌آزمایی‌شده در ۲۰۲۶/۰۷/۲۸ WordPress `7.0.2` است. نسخه‌های پیش‌انتشار خط مبنای Production نیستند.

## ۴. یک تأیید محصول بگیرید

خلاصه محصول در `docs/00-governance/PROJECT-CHARTER.md` ثبت می‌شود و مالک محصول فقط همان خلاصه را یک‌بار تأیید می‌کند.

پس از تأیید، اجرای فنی بدون تأیید مرحله‌به‌مرحله ادامه می‌یابد. مالک محصول نباید پیام‌رسان میان ChatGPT و Codex شود.

## ۵. سطح اجرا را تعیین کنید

### سبک

برای افزونه محدود، برگشت‌پذیر و بدون داده حساس. همه مراحل مهندسی انجام می‌شوند، اما اسناد، آزمون و شواهد فشرده‌اند.

### استاندارد

برای افزونه چندماژولی، داده معمولی، فرم، API، اتصال خارجی یا اثر واقعی بر کاربران. طراحی ثبت‌شده، CI، آزمون واقعی و راه بازگشت لازم است.

### بحرانی

برای پرداخت، اطلاعات حساس، احراز هویت، حذف گسترده، Migration دشوار یا پیامد جدی. امنیت، آزمون سوءاستفاده، شواهد و کنترل استقرار عمیق‌تر می‌شوند.

Codex سطح را تعیین و در خلاصه محصول اعلام می‌کند. این طبقه‌بندی تأیید جداگانه نمی‌خواهد.

## ۶. چرخه مهندسی را کامل اجرا کنید

هیچ مرحله‌ای حذف نمی‌شود:

1. مسئله و نتیجه
2. نیازمندی‌ها
3. معیارهای پذیرش
4. طراحی فنی
5. پیاده‌سازی
6. آزمون
7. بازبینی
8. بسته‌بندی
9. آمادگی استقرار
10. نصب یا بررسی پس از انتشار در محیط آزمایشی

اصل:

> مراحل ثابت، عمق شناور، اجرای خودمختار، تأیید انسانی استثنایی.

## ۷. اجرای مستقل Codex

Codex می‌تواند بدون توقف میانی:

- معماری و روش پیاده‌سازی را انتخاب کند؛
- کد، تست، CI و مستندات را بنویسد؛
- خطاهای عادی را تحلیل و رفع کند؛
- داده تست ایجاد و حذف کند؛
- Commit و Push معنادار انجام دهد؛
- در صورت ارزش واقعی PR بسازد؛
- ZIP، SHA-256، Tag و Release را آماده کند؛
- روی LocalWP، Docker یا Staging مجاز آزمون کند.

## ۸. سیاست ثابت WordPress را اعمال کنید

مرجع:

`docs/00-governance/WORDPRESS-PLUGIN-FACTORY-POLICY.md`

مهم‌ترین قواعد:

- PHP `8.1+`
- WordPress: آخرین نسخه پایدار رسمی ثبت‌شده در منشور؛ مقدار فعلی بررسی‌شده `7.0.2`
- عدم استفاده از نسخه پیش‌انتشار به‌عنوان حداقل Production
- فارسی و RTL، ولی ترجمه‌پذیر
- آمادگی WordPress.org
- بدون Telemetry پیش‌فرض
- حذف داده فقط با گزینه صریح
- نویسنده `رهبر بختیاری`
- مجوز `GPL-2.0-or-later`
- مخزن ابتدا Private

## ۹. محیط آزمون

ترتیب اجرا:

1. LocalWP موجود
2. تشخیص و تعمیر معقول LocalWP
3. Docker و محیط WordPress مستقل
4. آزمون جایگزین قوی و ثبت محدودیت، فقط وقتی اجرای واقعی ناممکن باشد

خرابی محیط دلیل رهاکردن پروژه نیست.

## ۱۰. نقاط توقف واقعی

فقط برای این موارد از مالک محصول تصمیم بگیر:

- ابهام واقعی محصول با پیامد کسب‌وکاری متفاوت
- Secret، ورود حساب یا مجوز انسانی
- هزینه یا خرید جدید
- حذف داده مهم یا اقدام برگشت‌ناپذیر
- اقدام پرخطر Production
- پذیرش خطر امنیتی، حقوقی یا سازمانی

## ۱۱. تحویل

بسته نهایی افزونه شامل این موارد است:

- ZIP نصب‌شدنی
- مخزن سالم، Tag و Release
- راهنمای فارسی نصب و استفاده
- `readme.txt` انگلیسی
- نتایج آزمون خودکار و واقعی
- نسخه‌های پشتیبانی‌شده
- SHA-256
- خطرها و محدودیت‌های واقعی

نصب نهایی روی Production و تغییر داده واقعی Production بر عهده مالک محصول است.
