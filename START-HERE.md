# راه‌اندازی نخستین پروژه با MASEF

## مرحله ۱ — ایجاد پروژه

از اسکریپت `bootstrap_project.py` استفاده کنید. این اسکریپت هسته چارچوب، پروفایل انتخابی، اسناد و قواعد Codex را در یک پوشه جدید قرار می‌دهد.

## مرحله ۲ — تکمیل پنج سند اجباری

تا زمانی که موارد زیر تکمیل نشده‌اند، کدنویسی ممنوع است:

1. `docs/00-governance/PROJECT-CHARTER.md`
2. `docs/00-governance/RISK-PROFILE.md`
3. `docs/01-product/REQUIREMENTS.md`
4. `docs/01-product/ACCEPTANCE-CRITERIA.md`
5. `docs/04-security/THREAT-MODEL.md`

## مرحله ۳ — اجرای مصاحبه نیازسنجی

پرامپت `prompts/01-intake-interview.md` را به Codex بدهید. عامل باید سؤال‌ها را یکی‌یکی بپرسد، فرض‌ها را ثبت کند و پیش از طراحی راه‌حل، تناقض‌ها را آشکار سازد.

## مرحله ۴ — طراحی و ثبت تصمیم‌ها

از پرامپت `prompts/03-architecture-proposal.md` استفاده کنید. هر تصمیم مهم باید در `docs/03-architecture/decisions/` به‌صورت ADR ثبت شود.

## مرحله ۵ — ایجاد برنامه اجرایی

برای هر قابلیت مهم، از قالب زیر استفاده شود:

`docs/05-execution/plans/active/PLAN-TEMPLATE.md`

برنامه باید مرحله‌های کوچک، آزمون هر مرحله، خطرها و راه بازگشت را مشخص کند.

## مرحله ۶ — اجرای Codex

قواعد مهم:

- ابتدا فقط برنامه و فایل‌های مرتبط را بخواند.
- روی شاخه جدا کار کند.
- پس از هر مرحله، تست اجرا کند.
- دامنه مأموریت را گسترش ندهد.
- تغییر پایگاه داده، وابستگی جدید یا دسترسی شبکه را بدون تأیید انجام ندهد.

## مرحله ۷ — بررسی مستقل

پرامپت `prompts/06-independent-review.md` باید در نشست یا عامل جدا اجرا شود. عامل بازبین نباید گزارش عامل سازنده را به‌عنوان حقیقت بپذیرد.

## مرحله ۸ — بسته شواهد

پیش از انتشار، فایل زیر تکمیل شود:

`docs/08-evidence/EVIDENCE-PACK-TEMPLATE.md`

## مرحله ۹ — انتشار و بازگشت

- محیط آزمایشی پیش از اصلی
- پشتیبان‌گیری تأییدشده
- نسخه‌گذاری
- دستور بازگشت آزمایش‌شده
- تأیید انسانی

## مرحله ۱۰ — یادگیری چارچوب

پس از هر خطای تکرارشونده، یکی از این موارد به‌روزرسانی شود:

- `AGENTS.md`
- تست خودکار
- Quality Gate
- Skill تخصصی
- سند تصمیم معماری

قانونی که فقط در گفت‌وگو باقی بماند، بخشی از Harness محسوب نمی‌شود.
