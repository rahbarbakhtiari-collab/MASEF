# شروع پروژه با MASEF 0.2

## ۱. سطح اجرا را انتخاب کنید

### سبک

برای افزونه کوچک، اسکریپت، ابزار شخصی یا تغییر برگشت‌پذیر بدون داده حساس.

حداقل لازم:

- شرح یک‌صفحه‌ای مسئله و نتیجه
- معیار پذیرش کوتاه
- پیاده‌سازی
- تست مرتبط
- Artifact یا روش نصب

### استاندارد

برای قابلیت چندفایلی، فرم، API، کاربر، داده معمولی یا اتصال خارجی.

لازم:

- منشور و نیازمندی کوتاه
- برنامه اجرایی مختصر
- تست و CI
- راه بازگشت متناسب
- یک نقطه تصمیم پیش از Production یا مهاجرت داده

### بحرانی

برای پرداخت، اطلاعات حساس، احراز هویت، حذف گسترده، سلامت، حقوق یا زیرساخت مهم.

لازم:

- اسناد کامل‌تر محصول، امنیت و عملیات
- کنترل‌های مستقل
- شواهد انتشار و Rollback آزمایش‌شده
- تأیید انسانی در نقاط پرخطر مشخص

## ۲. پروژه را بسازید

```bash
python scripts/bootstrap_project.py \
  --name "نام پروژه" \
  --slug "project-slug" \
  --profile wordpress-plugin \
  --risk light \
  --output ../project-slug
```

## ۳. نتیجه را روشن کنید

Codex باید از توضیح موجود، مسئله و خروجی قابل‌آزمون را استخراج کند. فقط ابهام‌های کسب‌وکاری مهم پرسیده شوند. سؤال فنی که از کد، محیط، مستندات یا آزمون قابل پاسخ است نباید به مالک محصول منتقل شود.

برای کار سبک، تکمیل این موارد کافی است:

- مسئله
- نتیجه مورد انتظار
- محدودیت واقعی
- معیار پذیرش

تکمیل همه قالب‌های مخزن پیش از شروع کدنویسی اجباری نیست.

## ۴. اجرا توسط Codex

Codex می‌تواند بدون توقف‌های میانی:

- طراحی فنی را انتخاب کند؛
- کد و تست را بنویسد؛
- خطاهای عادی را رفع کند؛
- مستندات لازم را به‌روزرسانی کند؛
- Commit، Push و Draft PR را روی شاخه کاری انجام دهد؛
- Artifact را آماده و روی محیط محلی یا آزمایشی مجاز بررسی کند.

## ۵. نقاط توقف واقعی

فقط برای این موارد از مالک محصول تصمیم بگیر:

- رفتار محصول واقعاً مبهم است؛
- Secret یا ورود شخصی لازم است؛
- هزینه یا سرویس پولی جدید مطرح است؛
- حذف داده یا تغییر برگشت‌ناپذیر لازم است؛
- اقدام پرخطر روی Production لازم است؛
- پذیرش ریسک امنیتی، حقوقی یا سازمانی لازم است.

## ۶. آزمون و تحویل

- سطح سبک: تست مرتبط و گزارش کوتاه
- سطح استاندارد: تست، CI، Smoke و راه بازگشت
- سطح بحرانی: مجموعه شواهد کامل‌تر و کنترل‌های مستقل

بازبینی مستقل، Staging جدا، Backup کامل و تمرین Rollback فقط وقتی اجباری‌اند که سطح خطر یا نوع تغییر واقعاً آن را توجیه کند.

## ۷. اصل یادگیری

هر بار فرایند از تحویل نرم‌افزار سنگین‌تر شد، قواعد باید ساده‌تر شوند. قانون خوب باید زمان تحویل را کاهش دهد و در عین حال از خطر واقعی جلوگیری کند.
