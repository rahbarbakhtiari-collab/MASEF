# AGENTS.md — قواعد اصلی Codex

## مأموریت

این مخزن بر پایه MASEF اداره می‌شود. هدف، اجرای فنی کامل توسط عامل با کنترل انسانی، شواهد قابل بررسی و امکان بازگشت است.

## ترتیب مطالعه پیش از کار

1. `docs/00-governance/PROJECT-CHARTER.md`
2. `docs/00-governance/RISK-PROFILE.md`
3. `docs/01-product/REQUIREMENTS.md`
4. `docs/01-product/ACCEPTANCE-CRITERIA.md`
5. `docs/03-architecture/ARCHITECTURE.md`
6. `docs/04-security/SECURITY.md`
7. برنامه فعال مرتبط در `docs/05-execution/plans/active/`
8. نزدیک‌ترین `AGENTS.md` در پوشه کاری

## قواعد اجرای کار

- قبل از تغییر چندفایلی یا پرخطر، برنامه اجرایی بنویس یا برنامه موجود را به‌روزرسانی کن.
- فقط در محدوده معیارهای پذیرش کار کن؛ تغییر غیرمرتبط ممنوع است.
- ابتدا کم‌خطرترین و برگشت‌پذیرترین راه را انتخاب کن.
- هر ادعای «تمام شد» باید همراه فرمان تست و نتیجه واقعی باشد.
- تست را برای سبزشدن مصنوعی تضعیف یا حذف نکن.
- خطا را با حدس پنهان نکن؛ علت، شواهد و محدودیت را ثبت کن.
- مستندات و کد باید در یک تغییر هماهنگ به‌روزرسانی شوند.
- هیچ کلید، رمز، توکن یا داده حساس را درخواست، چاپ یا Commit نکن.
- به فایل‌های `.env`، اسرار CI و داده اصلی فقط طبق سیاست امنیتی دسترسی داشته باش.
- افزودن وابستگی تولیدی، تغییر Schema پایگاه داده، دسترسی شبکه، حذف داده یا انتشار نیازمند تأیید است.
- شاخه اصلی را مستقیم تغییر نده؛ Pull Request و کنترل‌های CI الزامی است.

## پایان کار

کار فقط زمانی کامل است که:

- معیارهای پذیرش پوشش داده شده باشند؛
- تست‌های مرتبط موفق باشند؛
- بررسی امنیت متناسب با خطر انجام شده باشد؛
- مستندات به‌روز باشند؛
- راه بازگشت مشخص باشد؛
- بسته شواهد قابل تولید باشد.

## قالب گزارش نهایی عامل

1. خلاصه تغییر
2. فایل‌های تغییرکرده
3. معیارهای پذیرش پوشش‌داده‌شده
4. تست‌های اجراشده و نتیجه
5. خطرها و محدودیت‌های باقی‌مانده
6. تغییرات داده یا وابستگی‌ها
7. روش بازگشت
8. موارد نیازمند تأیید انسان
