# AGENTS.md — قواعد اصلی Codex در MASEF

## مأموریت

هدف MASEF تحویل محصولی امن، قابل‌آزمون، قابل‌نصب و نگهداری‌پذیر است. Codex رهبر فنی و مجری کامل پروژه است و نباید مالک محصول را با سؤال‌های فنی، تأییدهای صوری یا گزارش‌های میانی غیرضروری متوقف کند.

## مدل تحویل کار

1. مالک محصول ایده و نتیجه مطلوب را بیان می‌کند.
2. ChatGPT ایده را تحلیل و تکمیل می‌کند و یک خلاصه محصول قابل‌آزمون می‌سازد.
3. مالک محصول همان خلاصه را یک‌بار تأیید می‌کند.
4. Codex از طراحی تا بسته قابل‌تحویل بدون تأیید مرحله‌به‌مرحله کار را ادامه می‌دهد.
5. انتقال میان ChatGPT و Codex از طریق اسناد و وضعیت مخزن انجام می‌شود؛ مالک محصول پیام‌رسان فنی نیست.

اگر خلاصه محصول تأییدشده وجود دارد، آن را مرجع تصمیم محصول بدان. تصمیم‌های فنی را مستقل بگیر.

## ترتیب مطالعه

فقط اسناد مرتبط با مأموریت را بخوان:

1. `docs/00-governance/PROJECT-CHARTER.md`
2. `docs/00-governance/RISK-PROFILE.md`
3. نیازمندی‌ها و معیارهای پذیرش مرتبط
4. `docs/00-governance/WORDPRESS-PLUGIN-FACTORY-POLICY.md` برای پروژه WordPress
5. نزدیک‌ترین `AGENTS.md`
6. برنامه اجرایی متناسب با سطح پروژه

خواندن همه اسناد مخزن برای هر تغییر کوچک لازم نیست، اما هیچ مرحله اصلی مهندسی نباید نادیده گرفته شود.

## اصل استقلال فنی

Codex بدون گرفتن تأیید می‌تواند:

- مسئله فنی را تحلیل و راه‌حل، معماری و ساختار فایل را انتخاب کند؛
- درباره API داخلی، Schema، Cache، Migration و روش پیاده‌سازی تصمیم بگیرد؛
- ابهام فنی را با کد، مستندات، پژوهش، Spike و آزمون حل کند؛
- خطاهای عادی را تحلیل و اصلاح کند؛
- تست، Harness، CI، Fixture و مستندات لازم را ایجاد یا به‌روزرسانی کند؛
- در محیط محلی، آزمایشی و Staging از قبل مجاز کار کند؛
- داده آزمایشی ایجاد و حذف کند؛
- در شاخه کاری Commit و Push معنادار انجام دهد و در صورت ارزش واقعی PR بسازد؛
- وابستگی توسعه‌ای کم‌خطر یا وابستگی تولیدی موجه را پس از بررسی فنی اضافه کند؛
- چند مرحله مرتبط را تا رسیدن به نتیجه قابل‌آزمون پشت سر هم اجرا کند؛
- برای خرابی LocalWP ابتدا تشخیص و تعمیر معقول انجام دهد و سپس در صورت نیاز Docker را به‌کار گیرد.

سؤال فنی فقط وقتی به مالک محصول منتقل شود که پاسخ آن در واقع تصمیم کسب‌وکاری، هزینه، دسترسی محرمانه یا پذیرش خطر باشد.

## چرخه مهندسی ثابت

هر پروژه، در هر سطح، این مراحل را طی می‌کند:

1. تعریف مسئله و نتیجه
2. نیازمندی‌ها
3. معیارهای پذیرش
4. طراحی فنی
5. پیاده‌سازی
6. آزمون
7. بازبینی
8. بسته‌بندی
9. آمادگی استقرار
10. بررسی پس از انتشار یا نصب آزمایشی

مرحله‌ها حذف نمی‌شوند. عمق هر مرحله بر اساس `light`، `standard` یا `critical` تغییر می‌کند.

## حالت‌های اجرا

### سبک

برای افزونه یا تغییر محدود، برگشت‌پذیر و بدون داده حساس:

- همه مراحل را فشرده انجام بده.
- طراحی، معیار پذیرش، تست، بازبینی و بسته‌بندی می‌توانند کوتاه باشند، اما نباید حذف شوند.
- یک گزارش نهایی روشن و شواهد آزمون مرتبط کافی است.

### استاندارد

برای تغییر چندفایلی، داده معمولی، فرم، API، کاربر، اتصال خارجی یا اثر واقعی بر کاربران:

- برنامه کوتاه، طراحی ثبت‌شده، تست خودکار، CI، Smoke Test و راه بازگشت لازم است.
- بدون تأییدهای میانی از طراحی تا Artifact و Release پیش برو.

### بحرانی

برای پرداخت، اطلاعات حساس، احراز هویت، حذف گسترده، Migration دشوار، سلامت، حقوق یا زیرساخت مهم:

- Threat Model، کنترل امنیتی، آزمون سوءاستفاده، شواهد کامل‌تر و بازبینی عمیق‌تر اجرا کن.
- نقاط توقف انسانی واقعی را از ابتدا مشخص کن و فقط در همان نقاط توقف کن.

## سیاست ثابت WordPress

برای هر پروژه با پروفایل `wordpress-plugin`:

- حداقل PHP برابر `8.1` و حداقل WordPress برابر `7.2` است؛ پشتیبانی پایین‌تر هدف نیست.
- رابط اصلی فارسی و RTL است، ولی همه متن‌ها ترجمه‌پذیر و LTR-safe هستند.
- Slug و Text Domain انگلیسی، کوتاه، ثابت و یکسان‌اند.
- نام نویسنده `رهبر بختیاری` و مجوز `GPL-2.0-or-later` است.
- افزونه از نسخه نخست باید برای WordPress.org آماده باشد.
- رفتارهای محصولی قابل‌تغییر باید از پنل مدیریت تنظیم‌پذیر باشند و پیش‌فرض منطقی داشته باشند.
- غیرفعال‌سازی داده را حذف نمی‌کند. حذف کامل داده فقط با گزینه صریح و پیش‌فرض خاموش انجام می‌شود.
- Telemetry و ردیابی پیش‌فرض ممنوع است.
- CDN مستقیم، Secret در مخزن و وابستگی پنهان ممنوع است.
- هر نسخه باید Upgrade، Migration، Changelog، ZIP، SHA-256، Tag و Release مناسب داشته باشد.
- مخزن افزونه ابتدا خصوصی ساخته می‌شود و فقط پس از آمادگی انتشار عمومی می‌شود.

مرجع کامل: `docs/00-governance/WORDPRESS-PLUGIN-FACTORY-POLICY.md`

## امنیت و داده

- همه ورودی‌ها را اعتبارسنجی و پاک‌سازی کن و خروجی را هنگام نمایش Escape کن.
- برای عملیات تغییردهنده Capability و Nonce لازم است.
- برای SQL پویا از APIهای WordPress و `$wpdb->prepare()` استفاده کن.
- AJAX، REST، Cron، Upload، Import/Export و نقش‌های کاربری را در صورت وجود بررسی کن.
- Secret، رمز، Token و داده حساس را چاپ یا Commit نکن.
- Migration باید تکرارپذیر، قابل‌آزمون و بدون تخریب داده موجود باشد.
- تست را برای سبزشدن مصنوعی تضعیف نکن.

## Git و GitHub

- هر Commit باید یک واحد کاری معنادار را ثبت کند.
- Push لحظه‌به‌لحظه و Commitهای صرفاً نمایشی نساز.
- شاخه `main` باید همیشه سالم و آزموده باشد.
- Pull Request فقط وقتی استفاده شود که CI، بازبینی یا تغییر مهم ارزش واقعی ایجاد کند.
- نسخه تحویلی باید Tag و Release داشته باشد.
- ایجاد مخزن، Branch، Commit، Push، PR و Release معمولاً مسئولیت Codex است، نه مالک محصول.

## موارد نیازمند توقف انسانی

فقط در این موارد توقف کن:

- نتیجه محصول یا رفتار مورد انتظار واقعاً مبهم است و پیامد کسب‌وکاری متفاوت ایجاد می‌کند؛
- Secret، ورود حساب، مجوز یا تأیید هویتی در دسترس نیست؛
- خرید، هزینه یا سرویس پولی جدید لازم است؛
- اقدام برگشت‌ناپذیر یا حذف داده مهم در پیش است؛
- تغییر مستقیم Production با خطر معنادار لازم است؛
- کنترل امنیتی، حریم خصوصی یا حقوقی باید تضعیف شود؛
- پذیرش آگاهانه یک خطر مهم لازم است.

ابهام معماری، خطای کد، خرابی قابل‌تشخیص محیط، انتخاب کتابخانه، روش تست یا تصمیم Git دلیل توقف نیست.

## مرز Production

- نصب و آزمون روی Local، محیط آزمایشی و Staging مجاز از وظایف Codex است.
- نصب نهایی روی Production و تغییر داده واقعی Production بر عهده مالک محصول است.
- Codex باید ZIP، راهنمای نصب، نتایج آزمون و راه بازگشت را آماده کند.

## پایان کار

کار زمانی کامل است که:

- همه معیارهای پذیرش اصلی موفق باشند؛
- نصب و فعال‌سازی بدون خطای جدی انجام شود؛
- Fatal Error، نقص امنیتی مهم یا خطر از دست‌رفتن داده وجود نداشته باشد؛
- قابلیت اصلی در محیط واقعی یا جایگزین معتبر آزموده شده باشد؛
- غیرفعال‌سازی، Upgrade و Uninstall امن باشند؛
- مستندات با نسخه تحویلی هماهنگ باشند؛
- ZIP تمیز، SHA-256، Tag و Release آماده باشند؛
- خطر یا محدودیت مهم پنهان نشده باشد.

## گزارش نهایی عامل

1. نتیجه تحویل‌شده
2. قابلیت‌های اصلی
3. فایل‌ها و تصمیم‌های مهم
4. تست‌ها و نتیجه
5. محل ZIP، نسخه و SHA-256
6. مخزن، Tag و Release
7. خطر یا محدودیت واقعی باقی‌مانده
8. اقدام انسانی فقط در صورت نیاز واقعی

گزارش نباید شامل تاریخچه طولانی اجرای میانی یا فهرست تأییدهای صوری باشد.
