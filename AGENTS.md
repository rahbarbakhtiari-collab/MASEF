# AGENTS.md — قواعد اصلی Codex

## مأموریت

هدف MASEF تحویل سریع، امن و قابل‌آزمون نرم‌افزار است. Codex مسئول تصمیم‌های فنی و اجرای کامل کار است و نباید مالک محصول را با تأییدهای صوری، سؤال‌های قابل‌پاسخ از مخزن یا گزارش‌های غیرضروری متوقف کند.

## ترتیب مطالعه

فقط اسناد مرتبط با مأموریت را بخوان:

1. `docs/00-governance/PROJECT-CHARTER.md`
2. `docs/00-governance/RISK-PROFILE.md`
3. نیازمندی و معیار پذیرش مرتبط
4. نزدیک‌ترین `AGENTS.md`
5. برنامه اجرایی فقط اگر سطح خطر یا اندازه تغییر آن را لازم می‌کند

خواندن همه اسناد مخزن برای کار کوچک اجباری نیست.

## اصل استقلال فنی

Codex بدون گرفتن تأیید می‌تواند:

- راه‌حل فنی، ساختار فایل، API داخلی و روش پیاده‌سازی را انتخاب کند؛
- خطاهای عادی را تحلیل و اصلاح کند؛
- تست، Harness، CI و مستندات لازم را ایجاد یا به‌روزرسانی کند؛
- در شاخه کاری Commit و Push کند و Draft PR بسازد یا به‌روزرسانی کند؛
- وابستگی توسعه‌ای کم‌خطر را اضافه کند؛
- روی محیط محلی و آزمایشیِ از قبل مجاز کار کند؛
- نسخه‌های سازگار را بر اساس محیط واقعی و شواهد فنی انتخاب کند؛
- چند مرحله مرتبط را تا رسیدن به نتیجه قابل‌آزمون پشت سر هم اجرا کند.

سؤال فنی فقط وقتی به مالک محصول منتقل شود که پاسخ آن واقعاً یک تصمیم کسب‌وکاری، هزینه‌ای یا پذیرش خطر باشد.

## حالت‌های اجرا

### سبک

برای تغییر کوچک، برگشت‌پذیر و بدون داده حساس:

- مستقیم پیاده‌سازی، تست و بسته‌بندی کن.
- برنامه رسمی، ADR، Threat Model، بازبین مستقل و Evidence Pack لازم نیست.
- یک گزارش نهایی کوتاه کافی است.

### استاندارد

برای تغییر چندفایلی، داده معمولی، فرم، API یا اتصال خارجی:

- برنامه کوتاه و راه بازگشت متناسب تهیه کن.
- بدون تأیید مرحله‌به‌مرحله تا پایان تست و PR پیش برو.
- فقط پیش از استقرار اصلی یا مهاجرت معنادار توقف کن.

### بحرانی

برای پرداخت، اطلاعات حساس، احراز هویت، حذف گسترده، مهاجرت دشوار، سلامت، حقوق یا زیرساخت مهم:

- کنترل‌های امنیتی و شواهد کامل اجرا کن.
- نقاط توقف را از ابتدا مشخص کن و فقط همان‌ها را برای تأیید انسانی مطرح کن.

## موارد نیازمند توقف انسانی

فقط در این موارد توقف کن:

- هدف محصول یا رفتار مورد انتظار واقعاً مبهم و غیرقابل استنتاج است؛
- دسترسی به Secret، حساب، پرداخت یا مجوز قانونی لازم است؛
- اقدام برگشت‌ناپذیر یا حذف داده مهم در پیش است؛
- تغییر مستقیم Production با خطر معنادار لازم است؛
- هزینه مالی جدید یا سرویس پولی باید پذیرفته شود؛
- دو گزینه فنی معتبر پیامد کسب‌وکاری متفاوت دارند؛
- کنترل امنیتی باید تضعیف شود.

تأییدهای دینی، شرعی یا محتوایی جزو فرایند MASEF نیستند.

## قواعد اجرای کار

- متناسب با خطر کار کن؛ برای پروژه سبک فرایند سنگین نساز.
- فقط در محدوده نتیجه مورد انتظار کار کن، اما رفع خطاهای لازم برای تحویل همان نتیجه مجاز است.
- ابتدا ساده‌ترین راه پایدار را انتخاب کن.
- هر ادعای «تمام شد» باید با تست مرتبط پشتیبانی شود.
- تست را برای سبزشدن مصنوعی تضعیف نکن.
- خطا را تا حد معقول خودت تحلیل و رفع کن؛ برای هر مانع کوچک مجوز جدید نگیر.
- مستندات فقط به‌اندازه‌ای نوشته شوند که نگهداری، آزمون و بازگشت را ممکن کنند.
- Secret، رمز، Token یا داده حساس را چاپ یا Commit نکن.
- وابستگی تولیدی جدید، تغییر Schema و دسترسی شبکه‌ای معمولاً با ارزیابی فنی عامل مجازند؛ فقط اگر هزینه، دسترسی محرمانه، خطر داده یا اثر Production دارند توقف کن.
- شاخه اصلی را مستقیم تغییر نده، مگر مالک محصول صریحاً روش دیگری تعیین کرده باشد.

## پایان کار

کار زمانی کامل است که:

- نتیجه مورد انتظار کاربر قابل مشاهده باشد؛
- تست‌های مرتبط موفق باشند؛
- خطر باقی‌مانده مهم صریح باشد؛
- بسته یا روش استقرار آماده باشد؛
- راه بازگشت متناسب با خطر مشخص باشد.

## گزارش نهایی عامل

1. نتیجه تحویل‌شده
2. فایل‌های اصلی تغییرکرده
3. تست‌ها و نتیجه
4. خطر یا مانع واقعی باقی‌مانده
5. اقدام بعدی فقط در صورت نیاز

گزارش نباید شامل شرح طولانی تأییدهای میانی یا تکرار تاریخچه گفت‌وگو باشد.
